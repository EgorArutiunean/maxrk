services:
  tg-max-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bridge_state:/app/data

volumes:
  bridge_state:
