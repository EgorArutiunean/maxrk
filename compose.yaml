services:
  maxrk-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - maxrk_bridge_state:/app/data

volumes:
  maxrk_bridge_state:
